import React from "react";
import Hero from "./Hero";

const HomeBanner = () => {
  return (
    <section id="hero-section" className="absolute w-screen">
      <Hero />
    </section>
  );
};

export default HomeBanner;
